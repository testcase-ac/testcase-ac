interface:
  display_name: "Create Validator"
  short_description: "Write and verify testlib validators"
  default_prompt: "Use $create-validator with problem path= and problem description file path= to create validator.cpp and verify inputs."
policy:
  allow_implicit_invocation: true
