interface:
  display_name: "Create Checker"
  short_description: "Write and test testlib checkers"
  default_prompt: "Use $create-checker with problem path= and problem description file path= to write checker.cpp and validate targeted checker cases."
policy:
  allow_implicit_invocation: true
