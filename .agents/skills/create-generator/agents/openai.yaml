interface:
  display_name: "Create Generator"
  short_description: "Write and test testcase generators"
  default_prompt: "Use $create-generator with problem path= and problem description file path= to write and validate a C++ testlib generator."
policy:
  allow_implicit_invocation: true
