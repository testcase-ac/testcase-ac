# Generate a single test case consisting of 5000 digits,
# all '1', which is the maximum allowed length.
# This input is valid (never produces a '0'-only block),
# and yields the largest DP state size.
print("1" * 5000)
