#include "testlib.h"
using namespace std;
typedef long long ll;
 
int main(int argc, char* argv[]) {
	registerValidation(argc, argv);
 
	int N = inf.readInt(1, 200'000, "N"); inf.readSpace();
    int M = inf.readInt(1, 200'000, "M"); inf.readEoln();
	inf.readEof();
	return 0;
}

