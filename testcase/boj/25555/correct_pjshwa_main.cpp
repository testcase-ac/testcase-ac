#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using pii = pair<int, int>;

void fast_io() {
  cin.tie(nullptr)->sync_with_stdio(false);
}

const int MAX = 2e5;

void solve() {
  int N;
  cin >> N;

  // 플러시로 사용할 무늬 고르기
  // 순서대로  Spade, Heart, Diamond, Club
  for (int i = 0; i < (1 << 4); i++) {

  }
}

int main() {
  fast_io();

  int t = 1;
  // cin >> t;
  while (t--) solve();
}
