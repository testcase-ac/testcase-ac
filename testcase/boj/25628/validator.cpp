#include "testlib.h"
#include <bits/stdc++.h>
using namespace std;

int main(int argc, char* argv[]) {
    registerValidation(argc, argv);

    inf.readInt(1, 100, "A");
    inf.readSpace();
    inf.readInt(1, 100, "B");
    inf.readEoln();
    inf.readEof();
}
