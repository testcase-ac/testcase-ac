#include <cstdio>
#include <vector>

int main(){
	int t, n;
	scanf("%d", &t);
	for(int i = 0; i < t; i++){
		scanf("%d", &n);
		int x, y;
		scanf("%d%d", &x, &y)
		std::vector<std::pair<int,int>> vector_list;
		vector_list.push_back(std::make_pair(x, y));
		
	}
}