#include "testlib.h"
#include <bits/stdc++.h>
using namespace std;

int main(int argc, char* argv[]) {
    registerValidation(argc, argv);

    inf.readInt(10, 99, "x");
    inf.readSpace();
    inf.readInt(10, 99, "y");
    inf.readEoln();
    inf.readEof();
}
