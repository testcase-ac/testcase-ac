#include "testlib.h"
#include <bits/stdc++.h>
using namespace std;

int main(int argc, char* argv[]) {
    registerValidation(argc, argv);

    inf.readInt(2, 100000, "N");
    inf.readSpace();
    inf.readInt(2, 1000, "K");
    inf.readEoln();
    inf.readEof();
}
