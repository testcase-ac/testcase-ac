#include "testlib.h"
using namespace std;

int main(int argc, char* argv[]) {
    registerValidation(argc, argv);

    // Read N: 0 <= N <= 1e6
    int N = inf.readInt(0, 1000000, "N");
    inf.readEoln();
    inf.readEof();

    return 0;
}
