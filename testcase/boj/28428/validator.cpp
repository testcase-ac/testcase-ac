#include <bits/stdc++.h>
#include "testlib.h"
using namespace std;
using ll = long long;

int main(int argc, char *argv[]) {
  registerValidation(argc, argv);

  int nmax = 5000;
  int sx = inf.readInt(1, nmax, "sx"); inf.readSpace();
  int sy = inf.readInt(1, nmax, "sy"); inf.readSpace();
  int mx = inf.readInt(1, nmax, "mx"); inf.readSpace();
  int my = inf.readInt(1, nmax, "my"); inf.readEoln();
  inf.readEof();
}
