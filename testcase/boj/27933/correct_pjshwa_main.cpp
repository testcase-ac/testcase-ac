#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using pii = pair<int, int>;

void fast_io() {
  cin.tie(nullptr)->sync_with_stdio(false);
}

const int MAX = 5e5;
char C[MAX + 10];
int A[MAX + 1];

void solve() {
  int N, Q; cin >> N >> (C + 1);
  for (int i = 1; i <= N; i++) cin >> A[i];

  cin >> Q;
  while (Q--) {
    int l, r; cin >> l >> r;

  }
}

int main() {
  fast_io();

  int t = 1;
  // cin >> t;
  while (t--) solve();
}
