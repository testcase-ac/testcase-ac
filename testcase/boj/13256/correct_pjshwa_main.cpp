#include <bits/stdc++.h>
using namespace std;
using ll = long long;

void fast_io() {
  cin.tie(0)->sync_with_stdio(0);
}

const int MAX = 50;
int L[MAX], H[MAX];

void solve() {
  int N;
  cin >> N;
  for (int i = 0; i < N; i++) cin >> L[i] >> H[i];

}

int main() {
  fast_io();

  int t = 1;
  // cin >> t;
  while (t--) solve();
}
