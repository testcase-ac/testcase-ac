#include "testlib.h"
#include <bits/stdc++.h>
using namespace std;

int main(int argc, char* argv[]) {
    registerValidation(argc, argv);

    // Read single integer n
    int n = inf.readInt(1, 10000000, "n");
    inf.readEoln();

    // For this problem, validator only checks input format and constraints on n.
    // There is exactly one line, already read.

    inf.readEof();
}
