# Generate the maximum test case for the LIS problem:
# N = 1000, values from 1 to 1000 (strictly increasing),
# so the LIS is of length 1000 and is the sequence itself.

N = 1000
print(N)
print(*range(1, 1001))
