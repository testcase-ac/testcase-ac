#include <bits/stdc++.h> 
using namespace std;
using ll = long long;
using pii = pair<int, int>;

void fast_io() {
  cin.tie(nullptr)->sync_with_stdio(false);
}

ll n, i, j;
void solve() {
}

int main() {
  fast_io();

  while (cin >> n >> i >> j) {
    if (n == 0) break;
    solve();
  }
}
