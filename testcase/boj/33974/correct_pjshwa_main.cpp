#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using pii = pair<int, int>;

void fast_io() {
  cin.tie(nullptr)->sync_with_stdio(false);
}

void solve() {
  ll N, S, C; cin >> N >> S >> C;

  // solve
  // x + y = pw
  // Sx + Cy = N
  for (ll pw = 1; pw <= N; pw = 2 * pw + 1) {
    
  }

}

int main() {
  fast_io();

  int t = 1;
  cin >> t;
  while (t--) solve();
}
