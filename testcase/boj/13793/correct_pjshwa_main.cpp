#include <bits/stdc++.h>
using namespace std;

void fast_io() {
  cin.tie(nullptr)->sync_with_stdio(false);
}

const int MAX = 1000;

int A, B, P;
void solve() {
}

int main() {
  fast_io();

  while (cin >> A >> B >> P) {
    if (A == 0 && B == 0 && P == 0) break;
    solve();
  }
}
