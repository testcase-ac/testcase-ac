#include <bits/stdc++.h>
using namespace std;
#define int long long

int32_t main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int n1, k1, n2, k2;
    cin >> n1 >> k1 >> n2 >> k2;
    int ans = (n1 * k1 + n2 * k2);
    cout << ans;
}
