#include <bits/stdc++.h>
using namespace std;
using ll = long long;

void fast_io() {
  cin.tie(nullptr)->sync_with_stdio(false);
}

const int MAXW = 10000, MAXQ = 12000, MAXH = 1e18;
int P[MAXW + 1], cnt = 0;
int N, M, S, T;

void solve() {
  cin >> N >> M >> S >> T;
}

int main() {
  fast_io();

  while (cin >> N >> M >> S >> T) {
    if (N == 0 && M == 0 && S == 0 && T == 0) break;
    solve();
  }
}
