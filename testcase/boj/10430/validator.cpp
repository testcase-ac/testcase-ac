#include "testlib.h"
#include <bits/stdc++.h>
using namespace std;

int main(int argc, char* argv[]) {
    registerValidation(argc, argv);

    inf.readInt(2, 10000, "A");
    inf.readSpace();
    inf.readInt(2, 10000, "B");
    inf.readSpace();
    inf.readInt(2, 10000, "C");
    inf.readEoln();
    inf.readEof();
}
