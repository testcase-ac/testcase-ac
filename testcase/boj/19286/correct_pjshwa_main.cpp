#include <bits/stdc++.h>
using namespace std;
using ll = long long;

void fast_io() {
  cin.tie(nullptr)->sync_with_stdio(false);
}

const int MAXN = 1e5;
int x[MAXN], y[MAXN];

void solve() {
  int n; cin >> n;
  for (int i = 0; i < n; ++i) cin >> x[i] >> y[i];
  
}

int main() {
  fast_io();

  int t = 1;
  // cin >> t;
  while (t--) solve();
}
