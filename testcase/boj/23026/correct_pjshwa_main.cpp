#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using pii = pair<int, int>;

void fast_io() {
  cin.tie(0)->sync_with_stdio(0);
}

const int MAX = 4000;
ll a[MAX];

void solve() {
  ll n, b, r;
  cin >> n >> b >> r;
  for (int i = 0; i < n; i++) cin >> a[i];

  
}

int main() {
  fast_io();

  int t = 1;
  cin >> t;
  while (t--) solve();
}
