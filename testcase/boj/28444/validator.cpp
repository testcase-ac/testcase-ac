#include "testlib.h"
#include <bits/stdc++.h>
using namespace std;

int main(int argc, char* argv[]) {
    registerValidation(argc, argv);

    inf.readInt(0, 100, "H");
    inf.readSpace();
    inf.readInt(0, 100, "I");
    inf.readSpace();
    inf.readInt(0, 100, "A");
    inf.readSpace();
    inf.readInt(0, 100, "R");
    inf.readSpace();
    inf.readInt(0, 100, "C");
    inf.readEoln();
    inf.readEof();
}
