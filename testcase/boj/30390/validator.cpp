#include <bits/stdc++.h>
#include "testlib.h"
using namespace std;
using ll = long long;

int main(int argc, char *argv[]) {
  registerValidation(argc, argv);

  ll A = inf.readLong(1LL, (ll)1e10, "A"); inf.readSpace();
  ll B = inf.readLong(1LL, (ll)1e10, "B"); inf.readSpace();
  ll K = inf.readLong(1LL, (ll)1e10, "K"); inf.readEoln();

  inf.readEof();
}
